export type Item = {
  price : number
  name : string
  brand : string;
  productSKU : string;
  promo?: string;
  unitPrice: string;
  link: string;
  title: string;
  location: string;
}

export type Site = {
  link: string;
  title: string;
}