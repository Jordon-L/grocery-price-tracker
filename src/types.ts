export type Item = {
  price : number
  name : string
  brand : string;
  productSKU : string;
  tag: string;
  unit: string;
  location: string;
}

export type Site = {
  link: string;
  title: string;
}